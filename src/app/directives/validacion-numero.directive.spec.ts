import { ValidacionNumeroDirective } from './validacion-numero.directive';

describe('ValidacionNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
